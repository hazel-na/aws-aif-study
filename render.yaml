services:
  - type: web
    name: aws-aif-study
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    routes:
      # SPA(리액트 라우터) — 모든 경로를 index.html로 재작성
      - type: rewrite
        source: /*
        destination: /index.html
